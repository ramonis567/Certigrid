[package]
name = "certigrid_program"
version = "0.1.0"
description = "Certigrid renewable energy certificate traceability program."
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "certigrid_program"

[features]
default = []
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "1.0.0"
